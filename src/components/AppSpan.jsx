import React from "react";

export const AppSpan = ({desc}) => {
    return (
        <span className="indicator__description">
                Скидка за прохождение опроса:
              </span>
    )
}